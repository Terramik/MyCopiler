





def blob(var1 int32, var2 int32) { iprint(var1 * var2); }

def coss(v float32) -> (float32) {
    var v4 float32 = (var v2 float32 = v * v) * v2;
    return 1.0 - v2 / 2 + v4 / 24 - v4 * v2 / 720;
}



def main() -> (int32) {

    def _2_res() -> (int64, int32) {
        return 5.0, 10.0;
    }

    var bob int32 = ~1;
    bob, var tom int32 = _2_res();
    {
        var x float32;
        x = ( (-2)/(-2.0)*3+2 ); # x = 2+2-3;
        fprint(x-1+tom);
    }
    bob, var _F_ float64 = (tom + 10) as int32, ((3.1415/4) as float64) as float32;
    _F_ = _F_ = _F_;
    blob(tom, bob);
    iprint(tom); iprint(bob); bprint(not _F_ < 1 ); # commentaries is so commentary
    fprint(coss(_F_));
    # kill me
    # blob
    iprint( (true+true-false)*3 );

    def nth(n int64) -> (int64) {
        if n < 1 {
            return 0;
        } else {
            return n + nth(n - 1);
        }
    }
    iprint(nth(50));
    bprint(1 < 3 < 4 < 5 > 0 == 0);
    if 1 {  }
    if not false {  }

    {
        var res int64 = 0;
        var i int16 = 50;
        while true {
            res = res + i;
            i = i - 1;
            if i < 1 {
                break;
            }
        }
        iprint(res);
    }

    return 0;
}

