# MyCopiler

Простой компилятор из моего языка в С. Написан на Python.

Был создан после того, как я смог создать калькулятор, и делал я его потому что люблю делать прикольные штуки

Для документации самого языка см "LanguageDescriptions.html"

# Для запуска компилятора
Для начала, компилятор собирался при: Windows10, Python 3.13.7, gcc 15.2.0 и может не работать на вашей машине.

Для работы понадобиться компиляторы Python(3.10+) 0 С(c99+), и ar(архиватор) как минимум.

Чтобы запустить компилятор, запустите файл Main/Main.py в как модуль от родительской директории, где лежит весь компилятор.

Для начала, встроенная библиотека скомпилирована под Windows10, так что
выполните <запуск компилятора> retransfer_std_modules в консоли для её перекомпиляции.

Пример: python -m Test.TheLanguage.test2.Main.Main retransfer_std_modules

Тут(и при обычной компиляции) вы можете указать компилятор через --compiler=<имя компилятора в PATH> компилятор, а 
можете зайти в "Settings.json" и изменить тот, что будет использоваться по умолчанию.\

Пример: python -m Test.TheLanguage.test2.Main.Main retransfer_std_modules --compiler=gcc

Далее, запуск собственно компиляции. <запуск компилятора> <путь к входу> <путь к результату>.
Сначала введите путь к основному файл исходного кода(можно относительный, тогда как отправной путь будет использована 
директория, из которой в консоли запущен компилятор). И путь куда будет записан результат(директория должна 
быть пустой, т.к. будет полностью отчищена!)

Пример: python -m Test.TheLanguage.test2.Main.Main Test/TheLanguage/test2/scripts/game_of_life/main.mylang Test/TheLanguage/test2/scripts/result 

# Также, вы можете посмотреть работающие примеры:

Небольшая математическая библиотека с векторами, матрицами и кватернионами: scripts/Better/Math/

3д-движок с вращающимся в консоли тетраэдром: scripts/Better/Cube/

Простой калькулятор с только бинарными операторами: scripts/calculatator/


Лицензия: нет

И да, всё это может сломаться.

